- name: Deploy Log Viewer to a single node
  hosts: node1
  become: yes
  vars:
    app_dir: /opt/log_viewer
    service_name: log_viewer
    port: 27015
    repo_url: https://github.com/psych0panda/log_viewer.git  # Замените на ваш локальный домен
  tasks:
    - name: Install Python3 and pip
      apt:
        name:
          - python3
          - python3-pip
        state: present
        update_cache: yes

    - name: Install git
      apt:
        name: git
        state: present

    - name: Clone the repository
      git:
        repo: "{{ repo_url }}"
        dest: "{{ app_dir }}"
        version: main  # Укажите нужную ветку, если необходимо

    - name: Create virtualenv
      command: python3 -m venv {{ app_dir }}/venv

    - name: Install Python dependencies
      pip:
        requirements: "{{ app_dir }}/log_viewer/backend/requirements.txt"
        virtualenv: "{{ app_dir }}/venv"

    - name: Create systemd service file
      copy:
        dest: /etc/systemd/system/{{ service_name }}.service
        content: |
          [Unit]
          Description=Log Viewer Service
          After=network.target

          [Service]
          Type=simple
          User=root
          WorkingDirectory={{ app_dir }}/log_viewer/backend
          ExecStart={{ app_dir }}/venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port {{ port }}
          Restart=always

          [Install]
          WantedBy=multi-user.target
        owner: root
        group: root
        mode: '0644'
      notify: Restart log_viewer service

    - name: Enable and start the service
      systemd:
        name: "{{ service_name }}"
        enabled: yes
        state: started

  handlers:
    - name: Restart log_viewer service
      systemd:
        name: "{{ service_name }}"
        state: restarted
